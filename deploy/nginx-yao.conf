# 用药小应用（静态页，不碰 Hermes）
# 挂到已有 80 站点下的 /yao/，避免再开新端口。

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    location /yao/ {
        alias /var/www/yao/;
        index index.html;
        add_header Cache-Control "no-cache";
    }
}
